# caddy-desec
Custom Caddy builds for linux/amd64 and linux/arm64 platforms:

- `tag:latest` – Caddy with deSEC DNS provider module.
- `tag:latest-sablier` – Caddy with deSEC and Sablier modules. 
- `tag:latest-ratelimit` – Caddy with deSEC and mholt/caddy-ratelimit modules. 
